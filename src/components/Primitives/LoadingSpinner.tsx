import { SvgSpinners90Ring } from "@/components/Icons/SvgSpinners90Ring";

export const LoadingSpinner = ({ className }: { className?: string }) => {
    return <SvgSpinners90Ring className={className} />;
};
